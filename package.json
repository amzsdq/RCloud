{
  "name": "rcloud",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "node --test",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/playwright": "^1.3.0",
    "wrangler": "^4.0.0"
  }
}
